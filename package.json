{
  "name": "pnpm-trivy-repro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web:docker:build": "docker build . -t pnpm-trivy-repro-web -f apps/web/Dockerfile",
    "web:docker:run": "docker run --rm -p 3000:3000 pnpm-trivy-repro-web"
  },
  "devDependencies": {
    "prettier": "^3.5.2"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
